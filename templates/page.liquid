<section class="m-page-hero">
  <div class="m-page">
    <p class="m-eyebrow">Mireani</p>
    <h1>{{ page.title }}</h1>
    {% if page.content != blank %}
      <div class="m-page-intro">{{ page.content }}</div>
    {% endif %}
  </div>
</section>
